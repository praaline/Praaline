#ifndef METADATAWEBDESIGNER_H
#define METADATAWEBDESIGNER_H

#include <QObject>

class MetadataWebDesigner : public QObject
{
    Q_OBJECT
public:
    explicit MetadataWebDesigner(QObject *parent = nullptr);

signals:

public slots:
};

#endif // METADATAWEBDESIGNER_H